port: :9981

tls:
  # port: :1443
  # enabled: false
  cert_file: ./test-cert.pem
  key_file: ./test-key.pem
  acme:
    # enabled: false
    # port: :80
    host_whitelist: ["test.example.com"]
    cache_dir: "certs"

prefix: ""

secret_token: abc123

handlers:
  repository: ./repository/test-handler.sh
  registry: ./registry/test-handler.sh

notification:
  slack:
    # https://api.slack.com/incoming-webhooks
    incoming_webhook_url:  # export SLACK_URL
    channel_name: "#api-test"

    bitbucket_icon_url: "https://www.iconfinder.com/icons/1826176/download/png/256"
    github_icon_url: "https://www.iconfinder.com/icons/298822/download/png/256" # http://cdn.flaticon.com/png/256/25231.png

    # payload_template_repository: |
    #  hi, there! a repository seems changed.

    # payload_template_registry: |
    #  hi, there! an image seems changed.
