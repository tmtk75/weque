package consumer

import (
	"testing"

	"github.com/stretchr/testify/assert"
)

func TestRegistryConsumer(t *testing.T) {
	//ch := make(chan *registry.Webhook)
	//out := make(chan *slack.IncomingWebhook)
	//foo := make(chan Foo)
	//Start(nil, ch)
	assert.FailNow(t, "not implemented yet")
}
